"use client"; import Link from "next/link"; import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation";
export default function Join(){const [code,setCode]=useState(""),[error,setError]=useState("");const router=useRouter();function submit(e:FormEvent){e.preventDefault();const c=code.trim().toUpperCase();if(!localStorage.getItem("gift-exchange-"+c)){setError("We couldn’t find that exchange in this browser.");return}router.push("/exchange/"+c)}return <main className="formPage"><div className="formShell"><Link className="brand" href="/">Gift<span>Exchange</span></Link><p className="sectionLabel">JOIN AN EXCHANGE</p><h1>Someone saved<br/><em>you a spot.</em></h1><p className="lead">Enter the private invite code from your organizer.</p><form onSubmit={submit}><label>Invite code<input required value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="e.g. MERRY8"/></label>{error&&<p className="error">{error}</p>}<button className="button">Find my exchange <span>→</span></button></form></div></main>}